package graphs;

// Let's use our DiGraph. If projectA can't be done without ProjectB, projectB is a child of projectA.
public class Task07 {
//    public static String fullfilmentOf (DiGraph projects) {
//        ArrayList<DiGraph.Node> makeFirst = projects.getNodes()
//                                                    .stream()
//                                                    .filter(node -> node.getChildren().isEmpty())
//                                                    .collect(Collectors.toCollection(ArrayList::new));
//        for (DiGraph.Node node : makeFirst) {
//
//        }

//        ArrayList<DiGraph.Node> nodes = projects.getNodes();
//        while (!projects.getNodes().isEmpty()) {
//
//        }
//    }
}
